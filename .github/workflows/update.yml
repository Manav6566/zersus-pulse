name: Zersus Data Harvester

# This tells the action to run automatically at midnight UTC, 
# AND allows you to click the manual "Run workflow" button.
on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  update-feed:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install feedparser

      - name: Run dual-engine harvester
        run: python harvester.py

      - name: Commit and Push JSON payloads
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Zersus Automated Daemon"
          
          # Explicitly add all three files so the China dashboard works
          git add trends.json china_trends.json china_pulse.json
          
          git commit -m "CRON: Daily intelligence streams updated" || echo "No changes to commit"
          git push
